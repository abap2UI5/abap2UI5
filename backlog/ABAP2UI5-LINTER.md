<!-- Generated by .github/scripts/generate-backlog.mjs. Do not edit by hand:
     edit backlog/items/<id>.md, run `npm run backlog` and commit the result. -->

# abap2UI5 linter — rules about an app and its view

What the view linter could decide and does not. Its input is app classes and the view they build, so a candidate has to be decidable from those — a rule needing a running app or a system belongs in a skill, not here.

The linter is the only check that reaches a consumer's own app. A defect left as prose is a defect only the people who read the prose avoid.

Each item below is a whole file — motivation, current behaviour with source
references, the proposed change, an example — written so it can be **pasted
into an issue as it stands**. Converting one is a human step on purpose: the
stock fills itself so nothing is lost, and empties by hand so nothing is filed
that nobody stands behind. See [README](README.md) for the mechanism.

---

## Deferred

_a decision was made not to do this now, and why_

| Item | What | Priority | In stock since | Upstream |
|---|---|---|---|---|
| [`bound-aggregation-size-limit`](items/bound-aggregation-size-limit.md) | the 100-entry cap is normal UI5 behaviour and `cs_event-set_size_limit` already switches it — what is missing is the detection, because a port that forgets it renders truncated with no error anywhere and every gate stays green | medium | 2026-08-24 | abap2UI5/linter |

---

_Generated from `backlog/items/*.md` and the `**Backlog:**` lines in
`.claude/skills/` — `npm run backlog`._
