* =====================================================================
* GENERATED FILE - DO NOT EDIT (AGENTS.md rule 2)
* Source: app/webapp/controller/App.controller.js
* Embedded frontend resource, generated by tools/app2abap/trans2abap.js.
* Change the source file above and run 'npm run app2abap' to regenerate;
* the check_app2abap CI gate fails any manual edit here.
* =====================================================================
CLASS z2ui5_cl_ui5f_app_js DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.

    CLASS-METHODS get
      RETURNING
        VALUE(result) TYPE string.

  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_ui5f_app_js IMPLEMENTATION.

  METHOD get.

    result = `sap.ui.define(` && |\n| &&
             `  [` && |\n| &&
             `    "sap/ui/core/mvc/Controller",` && |\n| &&
             `    "z2ui5/controller/View1.controller",` && |\n| &&
             `    "z2ui5/core/Server",` && |\n| &&
             `    "z2ui5/core/AppState",` && |\n| &&
             `    "z2ui5/core/ViewSlots",` && |\n| &&
             `  ],` && |\n| &&
             `  (BaseController, Controller, Server, AppState, ViewSlots) => {` && |\n| &&
             `    "use strict";` && |\n| &&
             `    return BaseController.extend("z2ui5.controller.App", {` && |\n| &&
             `      onInit() {` && |\n| &&
             `        const state = AppState.state;` && |\n| &&
             `        state.oOwnerComponent = this.getOwnerComponent();` && |\n| &&
             `` && |\n| &&
             `        const manifest = state.oOwnerComponent.getManifest();` && |\n| &&
             `        const uri = manifest?.["sap.app"]?.dataSources?.http?.uri;` && |\n| &&
             `        AppState.setGlobal(` && |\n| &&
             `          "url",` && |\n| &&
             `          AppState.getGlobal("checkLocal") ? window.location.href : uri,` && |\n| &&
             `        );` && |\n| &&
             `` && |\n| &&
             `        for (const slot of ViewSlots.slots) {` && |\n| &&
             `          state[slot.controllerProp] = new Controller();` && |\n| &&
             `        }` && |\n| &&
             `        state.oApp = this.getView().byId("app");` && |\n| &&
             `` && |\n| &&
             `        Server.roundtrip();` && |\n| &&
             `      },` && |\n| &&
             `    });` && |\n| &&
             `  },` && |\n| &&
             `);` && |\n| &&
             `` && |\n| &&
              ``.

  ENDMETHOD.

ENDCLASS.
