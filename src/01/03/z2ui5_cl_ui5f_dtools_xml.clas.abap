* =====================================================================
* GENERATED FILE - DO NOT EDIT (AGENTS.md rule 2)
* Source: app/webapp/devtools/DeveloperTools.fragment.xml
* Embedded frontend resource, generated by tools/app2abap/trans2abap.js.
* Change the source file above and run 'npm run app2abap' to regenerate;
* the check_app2abap CI gate fails any manual edit here.
* =====================================================================
CLASS z2ui5_cl_ui5f_dtools_xml DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.

    CLASS-METHODS get
      RETURNING
        VALUE(result) TYPE string.

  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_ui5f_dtools_xml IMPLEMENTATION.

  METHOD get.

    result = `<core:FragmentDefinition` &&
             `    xmlns="sap.m"` &&
             `    xmlns:core="sap.ui.core"` &&
             `    xmlns:ce="sap.ui.codeeditor"` &&
             `>` &&
             `    <!-- The title is a plain model property, never an expression binding:` &&
             `         ``{= ... }`` is compiled with eval/new Function and dies under any` &&
             `         CSP stricter than the one the framework ships (AGENTS.md rule 13).` &&
             `         show() assembles the string. -->` &&
             `    <Dialog` &&
             `        title="{/title}"` &&
             `        stretch="true"` &&
             `        escapeHandler=".onEscape"` &&
             `    >` &&
             `        <!-- Six groups instead of twenty-two flat tabs. They are ordered` &&
             `             the way a debugging session runs: what is going on, what` &&
             `             broke, what went over the wire, what the screen is made of,` &&
             `             what it all runs on - and last the cross-tab search, which` &&
             `             is about all of them at once. Which sub-view each one offers` &&
             `             is decided by devtools/Tabs.js, not here. -->` &&
             `        <IconTabHeader` &&
             `            selectedKey="{/selectedGroup}"` &&
             `            select=".onGroupSelect"` &&
             `        >` &&
             `            <items>` &&
             `                <IconTabFilter` &&
             `                    text="Overview"` &&
             `                    key="OVERVIEW"` &&
             `                />` &&
             `                <IconTabFilter` &&
             `                    text="Problems"` &&
             `                    key="PROBLEMS"` &&
             `                    count="{/problemCount}"` &&
             `                />` &&
             `                <IconTabFilter` &&
             `                    text="Roundtrips"` &&
             `                    key="ROUNDTRIPS"` &&
             `                />` &&
             `                <IconTabFilter` &&
             `                    text="View &amp; Data"` &&
             `                    key="VIEWDATA"` &&
             `                />` &&
             `                <IconTabFilter` &&
             `                    text="System"` &&
             `                    key="SYSTEM"` &&
             `                />` &&
             `                <IconTabFilter` &&
             `                    text="Search"` &&
             `                    key="SEARCH"` &&
             `                />` &&
             `            </items>` &&
             `        </IconTabHeader>` &&
             `        <VBox>` &&
             `            <!-- One action bar for the whole dialog: the sub-view selector` &&
             `                 on the left, and on the right only the actions that belong` &&
             `                 to the group being shown. They used to sit in the footer,` &&
             `                 where Retry / Restart / Logout were offered on all` &&
             `                 twenty-two tabs including the twenty-one that have no` &&
             `                 error to retry. -->` &&
             `            <OverflowToolbar>` &&
             `                <!-- Search: answers "where does /CUSTOMER appear?" across` &&
             `                     every tab at once. It used to sit permanently in the` &&
             `                     dialog's sub-header, where a result silently replaced` &&
             `                     whatever tab was open underneath - as its own tab,` &&
             `                     entering and leaving the result is plain navigation.` &&
             `                     A fixed width, not "100%": OverflowToolbar cannot` &&
             `                     size a percentage-width child when it computes what` &&
             `                     overflows. -->` &&
             `                <SearchField` &&
             `                    value="{/searchTerm}"` &&
             `                    placeholder="Search across all tabs..."` &&
             `                    search=".onSearch"` &&
             `                    width="24rem"` &&
             `                    visible="{/isSearch}"` &&
             `                />` &&
             `                <!-- View & Data is two dimensions - which slot, and which` &&
             `                     aspect of it - rather than the eight flat tabs it was.` &&
             `                     Only the filled slots are offered. -->` &&
             `                <Label` &&
             `                    text="Slot"` &&
             `                    visible="{/showSlotBar}"` &&
             `                />` &&
             `                <SegmentedButton` &&
             `                    selectedKey="{/selectedSlot}"` &&
             `                    items="{/slots}"` &&
             `                    selectionChange=".onSlotSelect"` &&
             `                    visible="{/showSlotBar}"` &&
             `                >` &&
             `                    <items>` &&
             `                        <SegmentedButtonItem` &&
             `                            key="{key}"` &&
             `                            text="{text}"` &&
             `                        />` &&
             `                    </items>` &&
             `                </SegmentedButton>` &&
             `                <SegmentedButton` &&
             `                    selectedKey="{/selectedTab}"` &&
             `                    items="{/views}"` &&
             `                    selectionChange=".onViewSelect"` &&
             `                    visible="{/showViewBar}"` &&
             `                >` &&
             `                    <items>` &&
             `                        <SegmentedButtonItem` &&
             `                            key="{key}"` &&
             `                            text="{text}"` &&
             `                        />` &&
             `                    </items>` &&
             `                </SegmentedButton>` &&
             `                <ToolbarSpacer />` &&
             `                <!-- Overview: the one setting that belongs nowhere else. -->` &&
             `                <ToggleButton` &&
             `                    text="Open on Error"` &&
             `                    tooltip="Pop these tools open on the Log as soon as anything logs at error level. Off by default."` &&
             `                    pressed="{/openOnError}"` &&
             `                    press=".onToggleOpenOnError"` &&
             `                    visible="{/isOverview}"` &&
             `                />` &&
             `                <!-- Roundtrips: tier 2 of the recorder. Off by default -` &&
             `                     keeping request/response bodies is the only part of` &&
             `                     the history that costs real memory, so a production` &&
             `                     session must not pay for it unnoticed. -->` &&
             `                <ToggleButton` &&
             `                    text="Record Payloads"` &&
             `                    tooltip="Keep request/response bodies, so the Model Diff and View Diff work. 2 MB budget, oldest dropped first."` &&
             `                    pressed="{/recordPayloads}"` &&
             `                    press=".onToggleRecordPayloads"` &&
             `                    visible="{/isRoundtrips}"` &&
             `                />` &&
             `                <!-- Problems: the actions of the fatal-error overlay,` &&
             `                     shown with the error rather than everywhere. -->` &&
             `                <Button` &&
             `                    text="Retry"` &&
             `                    tooltip="Re-run the request that failed"` &&
             `                    press=".onErrorRetry"` &&
             `                    visible="{/hasRetry}"` &&
             `                />` &&
             `                <Button` &&
             `                    text="Restart"` &&
             `                    tooltip="Reload the page"` &&
             `                    press=".onErrorRestart"` &&
             `                    visible="{/isErrorView}"` &&
             `                />` &&
             `                <Button` &&
             `                    text="Logout"` &&
             `                    press=".onErrorLogout"` &&
             `                    visible="{/isErrorView}"` &&
             `                />` &&
             `                <!-- View & Data: closes the dialog, lets the user click any` &&
             `                     control in the app and reopens with its bindings and` &&
             `                     current values (devtools/Picker.js). -->` &&
             `                <Button` &&
             `                    text="Pick Control"` &&
             `                    type="Emphasized"` &&
             `                    tooltip="Closes these tools, then click any control in the app. Escape cancels."` &&
             `                    press=".onPickControl"` &&
             `                    visible="{/isViewData}"` &&
             `                />` &&
             `                <ToggleButton` &&
             `                    text="After Templating"` &&
             `                    tooltip="Show the XML after XML templating ran"` &&
             `                    pressed="{/templatingSource}"` &&
             `                    press=".onTemplatingPress"` &&
             `                    visible="{/isTemplating}"` &&
             `                />` &&
             `                <!-- Live view editing: only on the sub-views that map to a` &&
             `                     view slot (devtools/LiveEdit.js sets canApply). Apply` &&
             `                     renders the edited XML into the running app WITHOUT a` &&
             `                     roundtrip - a local preview the next response` &&
             `                     replaces again. -->` &&
             `                <Button` &&
             `                    text="Apply to App"` &&
             `                    type="Emphasized"` &&
             `                    tooltip="Render the edited XML into the running app - no roundtrip, no activation"` &&
             `                    press=".onApplyXml"` &&
             `                    visible="{/canApply}"` &&
             `                />` &&
             `                <Button` &&
             `                    text="Reset"` &&
             `                    tooltip="Put the backend's original XML back"` &&
             `                    press=".onResetXml"` &&
             `                    visible="{/canApply}"` &&
             `                />` &&
             `                <!-- System: the inline frame below is only a preview - some` &&
             `                     systems block framing the ADT endpoint, and the ADT` &&
             `                     jump link inside it never navigates. This opens the` &&
             `                     same source in a new tab, where its jump works. -->` &&
             `                <Button` &&
             `                    text="Open in ADT"` &&
             `                    tooltip="Open the ABAP class in a new tab, at the line of the last event"` &&
             `                    press=".onOpenAbapInAdt"` &&
             `                    visible="{/isSourceView}"` &&
             `                />` &&
             `            </OverflowToolbar>` &&
             `            <!-- The result of Apply / Report a Bug. In the content area,` &&
             `                 not as a toast: this dialog is modal, so a toast behind it` &&
             `                 would be invisible. -->` &&
             `            <Text` &&
             `                text="{/statusText}"` &&
             `                visible="{/hasStatusText}"` &&
             `                class="sapUiTinyMarginBottom"` &&
             `            />` &&
             `            <ce:CodeEditor` &&
             `                id="developerToolsEditor"` &&
             `                type="{/type}"` &&
             `                value="{/value}"` &&
             `                height="2000px"` &&
             `                width="10000px"` &&
             `                visible="{/editor_visible}"` &&
             `            />` &&
             `        </VBox>` &&
             `        <VBox visible="{/source_visible}">` &&
             `            <!-- preferDOM defaults to true, which restores the preserved` &&
             `                 iframe DOM of a previous open instead of the freshly set` &&
             `                 content - the ABAP Source view then kept showing the class` &&
             `                 of the previously running app. -->` &&
             `            <core:HTML id="sourceHtml" preferDOM="false"/>` &&
             `        </VBox>` &&
             `        <!-- sap.m.Dialog only gained a public ``footer`` aggregation around` &&
             `             1.110; on older releases (e.g. 1.71) a <footer> tag is resolved` &&
             `             as a control and fails with "failed to load sap/m/footer.js".` &&
             `             The ``buttons`` aggregation (since 1.21.1) is the cross-version` &&
             `             footer; UI5 lays the buttons out in an overflow toolbar.` &&
             `` &&
             `             Only what is global belongs here. Everything that serves one` &&
             `             group moved into the action bar above. -->` &&
             `        <buttons>` &&
             `            <!-- message-information, not information: the plain ``information``` &&
             `                 glyph was added to the SAP icon font after 1.71, so on the` &&
             `                 oldest release abap2UI5 supports the button renders with no` &&
             `                 icon at all. message-information is the same circled "i" and` &&
             `                 exists in every release from 1.71 on. -->` &&
             `            <Button` &&
             `                icon="sap-icon://message-information"` &&
             `                tooltip="Help - what every tab answers and how to report a bug"` &&
             `                press=".onShowHelp"` &&
             `            />` &&
             `            <Button` &&
             `                text="Copy"` &&
             `                tooltip="Put what is shown on the clipboard"` &&
             `                press=".onCopyTab"` &&
             `            />` &&
             `            <!-- The most common reason these tools are opened at all, so` &&
             `                 it is one click and emphasized: the whole session state on` &&
             `                 the clipboard as a GitHub-ready issue body. -->` &&
             `            <Button` &&
             `                text="Report a Bug"` &&
             `                type="Emphasized"` &&
             `                tooltip="Copy the whole session state as a GitHub issue body"` &&
             `                press=".onReportBug"` &&
             `            />` &&
             `            <Button` &&
             `                text="Export"` &&
             `                tooltip="Read the same report, with downloads"` &&
             `                press=".onExport"` &&
             `            />` &&
             `            <Button` &&
             `                text="Close"` &&
             `                press=".onClose"` &&
             `            />` &&
             `        </buttons>` &&
             `    </Dialog>` &&
             `</core:FragmentDefinition>` &&
             `` &&
              ``.

  ENDMETHOD.

ENDCLASS.
