* =====================================================================
* GENERATED FILE - DO NOT EDIT (AGENTS.md rule 2)
* Source: app/webapp/cc/Dirty.js
* Embedded frontend resource, generated by tools/app2abap/trans2abap.js.
* Change the source file above and run 'npm run app2abap' to regenerate;
* the check_app2abap CI gate fails any manual edit here.
* =====================================================================
CLASS z2ui5_cl_ui5f_dirty_js DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.

    CLASS-METHODS get
      RETURNING
        VALUE(result) TYPE string.

  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_ui5f_dirty_js IMPLEMENTATION.

  METHOD get.

    result = `sap.ui.define(` && |\n| &&
             `  ["sap/ui/core/Control", "z2ui5/core/Lib", "z2ui5/core/AppState"],` && |\n| &&
             `  (Control, Lib, AppState) => {` && |\n| &&
             `    "use strict";` && |\n| &&
             `` && |\n| &&
             `    const dirtyControls = new Set();` && |\n| &&
             `` && |\n| &&
             `    function syncUnloadPrompt(anyDirty) {` && |\n| &&
             `      window.onbeforeunload = anyDirty` && |\n| &&
             `        ? (e) => {` && |\n| &&
             `            e.preventDefault();` && |\n| &&
             `            e.returnValue = "";` && |\n| &&
             `          }` && |\n| &&
             `        : null;` && |\n| &&
             `    }` && |\n| &&
             `` && |\n| &&
             `    const Dirty = Control.extend("z2ui5.cc.Dirty", {` && |\n| &&
             `      metadata: {` && |\n| &&
             `        properties: {` && |\n| &&
             `          isDirty: {` && |\n| &&
             `            type: "boolean",` && |\n| &&
             `            defaultValue: false,` && |\n| &&
             `          },` && |\n| &&
             `        },` && |\n| &&
             `      },` && |\n| &&
             `      setIsDirty(val) {` && |\n| &&
             `        this.setProperty("isDirty", val, true);` && |\n| &&
             `        if (val) {` && |\n| &&
             `          dirtyControls.add(this);` && |\n| &&
             `        } else {` && |\n| &&
             `          dirtyControls.delete(this);` && |\n| &&
             `        }` && |\n| &&
             `        this._applyDirtyState();` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      _applyDirtyState() {` && |\n| &&
             `        const anyDirty = dirtyControls.size > 0;` && |\n| &&
             `        try {` && |\n| &&
             `          const launchpad = AppState.state.oLaunchpad;` && |\n| &&
             `          const hasFlpDirtyFlag =` && |\n| &&
             `            launchpad?.Container?.setDirtyFlag && launchpad.ShellUIService;` && |\n| &&
             `          if (hasFlpDirtyFlag) {` && |\n| &&
             `            launchpad.Container.setDirtyFlag(anyDirty);` && |\n| &&
             `` && |\n| &&
             `            syncUnloadPrompt(false);` && |\n| &&
             `          } else {` && |\n| &&
             `            syncUnloadPrompt(anyDirty);` && |\n| &&
             `          }` && |\n| &&
             `        } catch (e) {` && |\n| &&
             `          Lib.logError("Dirty._applyDirtyState: setDirtyFlag failed", e);` && |\n| &&
             `          syncUnloadPrompt(anyDirty);` && |\n| &&
             `        }` && |\n| &&
             `      },` && |\n| &&
             `      exit() {` && |\n| &&
             `        dirtyControls.delete(this);` && |\n| &&
             `        this._applyDirtyState();` && |\n| &&
             `      },` && |\n| &&
             `      renderer: Lib.EMPTY_RENDERER,` && |\n| &&
             `    });` && |\n| &&
             `` && |\n| &&
             `    Dirty.reset = function reset() {` && |\n| &&
             `      dirtyControls.clear();` && |\n| &&
             `      syncUnloadPrompt(false);` && |\n| &&
             `    };` && |\n| &&
             `` && |\n| &&
             `    return Dirty;` && |\n| &&
             `  },` && |\n| &&
             `);` && |\n| &&
             `` && |\n| &&
              ``.

  ENDMETHOD.

ENDCLASS.
