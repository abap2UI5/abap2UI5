* =====================================================================
* GENERATED FILE - DO NOT EDIT (AGENTS.md rule 2)
* Source: app/webapp/embed/Container.css
* Embedded frontend resource, generated by tools/app2abap/trans2abap.js.
* Change the source file above and run 'npm run app2abap' to regenerate;
* the check_app2abap CI gate fails any manual edit here.
* =====================================================================
CLASS z2ui5_cl_ui5f_embed_css DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.

    CLASS-METHODS get
      RETURNING
        VALUE(result) TYPE string.

  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_ui5f_embed_css IMPLEMENTATION.

  METHOD get.

    result = `/* z2ui5.embed.Container - loaded once by Container.js */` &&
             `` &&
             `.z2ui5EmbedContainer {` &&
             `  /* the embedded sap.m.App and its absolutely positioned pages stay inside` &&
             `     the area the host gave the control */` &&
             `  position: relative;` &&
             `  overflow: hidden;` &&
             `}` &&
             `` &&
             `/* sap.m.Shell - the root of many abap2UI5 views, the hello world app's too -` &&
             `   limits the app width by a media query on the VIEWPORT: from 1280px on,` &&
             `   html.sap-desktop .sapMShellAppWidthLimited .sapMShellCentralBox is 1280px` &&
             `   wide and centered with left: 50% and a negative margin. Inside a container` &&
             `   narrower than the viewport that pushes the app out to the left. Here the` &&
             `   container is the "screen", so the box fills it. The selector has to` &&
             `   outweigh sap.m's (0,3,1), hence the doubled Shell class: (0,4,0). */` &&
             `.z2ui5EmbedContainer .sapMShell.sapMShellAppWidthLimited .sapMShellCentralBox {` &&
             `  left: 0;` &&
             `  margin-left: 0;` &&
             `  width: 100%;` &&
             `}` &&
             `` &&
              ``.

  ENDMETHOD.

ENDCLASS.
