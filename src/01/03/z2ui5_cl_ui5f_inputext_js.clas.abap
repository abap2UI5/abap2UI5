* =====================================================================
* GENERATED FILE - DO NOT EDIT (AGENTS.md rule 2)
* Source: app/webapp/cc/InputExt.js
* Embedded frontend resource, generated by tools/app2abap/trans2abap.js.
* Change the source file above and run 'npm run app2abap' to regenerate;
* the check_app2abap CI gate fails any manual edit here.
* =====================================================================
CLASS z2ui5_cl_ui5f_inputext_js DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.

    CLASS-METHODS get
      RETURNING
        VALUE(result) TYPE string.

  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_ui5f_inputext_js IMPLEMENTATION.

  METHOD get.

    result = `sap.ui.define(` && |\n| &&
             `  ["sap/m/Input", "sap/m/InputRenderer", "z2ui5/core/Lib"],` && |\n| &&
             `  (Input, InputRenderer, Lib) => {` && |\n| &&
             `    "use strict";` && |\n| &&
             `` && |\n| &&
             `    const HTML_MODES = new Set([` && |\n| &&
             `      "decimal",` && |\n| &&
             `      "email",` && |\n| &&
             `      "none",` && |\n| &&
             `      "numeric",` && |\n| &&
             `      "search",` && |\n| &&
             `      "tel",` && |\n| &&
             `      "text",` && |\n| &&
             `      "url",` && |\n| &&
             `    ]);` && |\n| &&
             `` && |\n| &&
             `    return Input.extend("z2ui5.cc.InputExt", {` && |\n| &&
             `      metadata: {` && |\n| &&
             `        properties: {` && |\n| &&
             `          inputMode: {` && |\n| &&
             `            type: "string",` && |\n| &&
             `            defaultValue: "",` && |\n| &&
             `          },` && |\n| &&
             `        },` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      setInputMode(val) {` && |\n| &&
             `        this.setProperty("inputMode", val, true);` && |\n| &&
             `        this._applyInputMode();` && |\n| &&
             `        return this;` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      onAfterRendering(...args) {` && |\n| &&
             `        Input.prototype.onAfterRendering.apply(this, args);` && |\n| &&
             `` && |\n| &&
             `        const dom = this.getFocusDomRef();` && |\n| &&
             `        this._renderedMode = dom ? dom.getAttribute("inputmode") : null;` && |\n| &&
             `        this._applyInputMode();` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      _htmlMode() {` && |\n| &&
             `        const raw = this.getInputMode();` && |\n| &&
             `        if (!raw) return "";` && |\n| &&
             `` && |\n| &&
             `        const mode = String(raw).trim().toLowerCase();` && |\n| &&
             `        if (HTML_MODES.has(mode)) return mode;` && |\n| &&
             `` && |\n| &&
             `        if (this._refusedMode !== mode) {` && |\n| &&
             `          this._refusedMode = mode;` && |\n| &&
             `          Lib.logError(` && |\n| &&
             `            ``InputExt: inputMode "${raw}" is not an HTML inputmode keyword - `` +` && |\n| &&
             `              ``ignored``,` && |\n| &&
             `          );` && |\n| &&
             `        }` && |\n| &&
             `        return "";` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      _applyInputMode() {` && |\n| &&
             `        const dom = this.getFocusDomRef();` && |\n| &&
             `        if (!dom) return;` && |\n| &&
             `        const mode = this._htmlMode();` && |\n| &&
             `        if (mode) {` && |\n| &&
             `          dom.setAttribute("inputmode", mode);` && |\n| &&
             `        } else if (this._renderedMode) {` && |\n| &&
             `          dom.setAttribute("inputmode", this._renderedMode);` && |\n| &&
             `        } else {` && |\n| &&
             `          dom.removeAttribute("inputmode");` && |\n| &&
             `        }` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      renderer: InputRenderer,` && |\n| &&
             `    });` && |\n| &&
             `  },` && |\n| &&
             `);` && |\n| &&
             `` && |\n| &&
              ``.

  ENDMETHOD.

ENDCLASS.
