* =====================================================================
* GENERATED FILE - DO NOT EDIT (AGENTS.md rule 2)
* Source: app/webapp/cc/ScanInput.js
* Embedded frontend resource, generated by tools/app2abap/trans2abap.js.
* Change the source file above and run 'npm run app2abap' to regenerate;
* the check_app2abap CI gate fails any manual edit here.
* =====================================================================
CLASS z2ui5_cl_ui5f_scaninp_js DEFINITION
  PUBLIC
  FINAL
  CREATE PUBLIC .

  PUBLIC SECTION.

    CLASS-METHODS get
      RETURNING
        VALUE(result) TYPE string.

  PROTECTED SECTION.
  PRIVATE SECTION.
ENDCLASS.


CLASS z2ui5_cl_ui5f_scaninp_js IMPLEMENTATION.

  METHOD get.

    result = `sap.ui.define(` && |\n| &&
             `  ["sap/m/Input", "sap/m/InputRenderer"],` && |\n| &&
             `  (Input, InputRenderer) => {` && |\n| &&
             `    "use strict";` && |\n| &&
             `` && |\n| &&
             `    // A sap.m.Input that carries the HTML ``inputmode`` of its field as a` && |\n| &&
             `    // PROPERTY. inputmode="none" is what keeps the soft keyboard down on a` && |\n| &&
             `    // scanner device while the field still takes the scanner's input.` && |\n| &&
             `    //` && |\n| &&
             `    // UI5 has no property for it, so the only other way to set it is to write` && |\n| &&
             `    // the attribute onto the DOM - and every re-render throws that DOM away.` && |\n| &&
             `    // That is what the KEYBOARD_SET_MODE action has to work around: it applies` && |\n| &&
             `    // the attribute from a delegate and has to keep re-applying it, because` && |\n| &&
             `    // the mode it sets is not part of what the control IS. Here it is: the` && |\n| &&
             `    // control writes it whenever it renders, so no re-render can lose it and` && |\n| &&
             `    // no follow-up action has to arrive in the right order.` && |\n| &&
             `    //` && |\n| &&
             `    // A BOUND mode is the point of the property: "keyboard on/off" becomes a` && |\n| &&
             `    // plain model update, with no action travelling at all.` && |\n| &&
             `    return Input.extend("z2ui5.cc.ScanInput", {` && |\n| &&
             `      metadata: {` && |\n| &&
             `        properties: {` && |\n| &&
             `          // The HTML inputmode: "none" hides the soft keyboard, "text",` && |\n| &&
             `          // "numeric", "decimal", "tel", ... restore it with that layout.` && |\n| &&
             `          // Empty leaves the attribute off entirely, which makes this control` && |\n| &&
             `          // a plain sap.m.Input - so a bound mode can switch the behaviour off` && |\n| &&
             `          // again without the app having to remove anything.` && |\n| &&
             `          inputMode: {` && |\n| &&
             `            type: "string",` && |\n| &&
             `            defaultValue: "",` && |\n| &&
             `          },` && |\n| &&
             `        },` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      // Written WITHOUT invalidating: the mode changes one DOM attribute and` && |\n| &&
             `      // nothing UI5 renders from it, so a bound mode must not cost a re-render` && |\n| &&
             `      // of the input (and of its suggestion list) on every toggle. The write` && |\n| &&
             `      // below reaches the live DOM directly; onAfterRendering covers the case` && |\n| &&
             `      // where there is none yet.` && |\n| &&
             `      setInputMode(val) {` && |\n| &&
             `        this.setProperty("inputMode", val, true);` && |\n| &&
             `        this._applyInputMode();` && |\n| &&
             `        return this;` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      onAfterRendering(...args) {` && |\n| &&
             `        Input.prototype.onAfterRendering.apply(this, args);` && |\n| &&
             `        this._applyInputMode();` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      _applyInputMode() {` && |\n| &&
             `        // the inner <input>, which is what carries the attribute - not the` && |\n| &&
             `        // control's outer DOM root` && |\n| &&
             `        const dom = this.getFocusDomRef();` && |\n| &&
             `        if (!dom) return;` && |\n| &&
             `        const mode = this.getInputMode();` && |\n| &&
             `        if (mode) {` && |\n| &&
             `          dom.setAttribute("inputmode", mode);` && |\n| &&
             `        } else {` && |\n| &&
             `          dom.removeAttribute("inputmode");` && |\n| &&
             `        }` && |\n| &&
             `      },` && |\n| &&
             `` && |\n| &&
             `      renderer: InputRenderer,` && |\n| &&
             `    });` && |\n| &&
             `  },` && |\n| &&
             `);` && |\n| &&
             `` && |\n| &&
              ``.

  ENDMETHOD.

ENDCLASS.
